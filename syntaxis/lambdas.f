Animal = Generic
<Generic> Accion = (Generic -> Nothing) Lambda
 / el <Animal> / puede hacer una <Animal> Accion =
    random word "Animal" accion

Gato =
    nombre = Text,
    edad = Number

f = 
    for each (1 ... 4):
        / el <gato> / puede:
            text (gato, " puede lamer.")